# 宣纸留白

2026-09-10，用户确定整体 UI 使用「宣纸留白」，并明确要求同步修改视频色彩。

## 统一范围

- 主站欢迎页、问卷、个人 Skill、工作台、学习库、作品页与弹窗使用同一套纸色、墨色、朱砂色。状态成功与警告仍保留语义颜色。
- 标题沿用宋体方向，正文与操作保持清晰的无衬线字体。主标题不再整行染强调色；封面下的深色操作条改为浅纸面。
- 选择确认动效移除过冲弹跳；保留渐入、进度过渡及减少动态设置。
- 《窗口期可能只剩五年》案例的各个阅读形式、移动端图解、10 张可下载图解、封面和真实 MP4 同步改色。
- 视频画布为纸色，标题墨色，重点使用细朱砂线与淡底色，不再使用霓虹描边和发光。
- `server/player.mjs`、`server/study-export.mjs` 更新，后续新生成的视频、网页讲解、SVG 图解和离线作品继承新主题。

## 配色

纸面 `#f5f1e8`，浅表面 `#fbf8f1`，墨色 `#2b2926`，次级文字 `#736b61`，朱砂 `#b3402a`，深朱砂 `#8d301f`，边线 `#ded7c9`。

## 保留边界

原文、章节、口播、已有音频与时间轴不变，不重新调用模型或配音服务。原作者推演、虚构教学情境和事实边界保留。私人学习库、Cookie、个人 Skill 和笔记不做批量改写；历史私人视频保持原文件，新生成视频使用新主题。

案例更新通过资源版本参数避免旧视频、封面、图解缓存混用。旧版服务器发布目录与本地媒体备份保留，备份不进入公开目录。

## 验证方式

主站使用单元、HTTP 隔离、组件服务端渲染、类型检查、规范检查和生产构建验证。案例测试比较旧版章节与新版章节的口播、内容、图解关系及几何布局，仅允许颜色与字体改变；使用原始附件核对来源。视频重新导出后检查时长、编码、音轨和完整解码，查看导出画面。

本次没有执行网站浏览器交互验收；不能将组件测试或视频导出画面检查表述为整个网站的浏览器验收。

本地验证：主站 35 项测试、类型与规范检查通过；案例完整 23 项测试使用原始附件与媒体检查运行。新 MP4 为 1280 × 720、24 fps、H.264/AAC，时长 408.8 秒，完整解码无错误。新视频音轨与原音频流 SHA-256 均为 `aea71cdaea9ac07413122d03e21e57631cb851d4527c1a377b898d992efbe6c1`；新 MP4 SHA-256 为 `7491dec05337ff70b0f7e7ff82eda77935da2cab0887b0978bb6305ac5a02d5d`。查看了新视频首帧和第 8 章导出图，确认纸面、墨字、朱砂强调及取消发光边框生效。

## 发布结果

已发布至用户服务器 `/home/ubuntu/apps/zhijing/releases/20260910-rice-paper`，旧版 `20260910-article-case` 保留。空队列检查后创建私有数据库一致性快照，再切换并重启本项目网页与 API；未修改 Nginx、供应商配置或其他项目。

公网主页与案例页正常；26 个新版 JS/CSS、封面、视频与图解资源的公网字节哈希与本地一致，视频 Range 请求返回 206。两个服务 active，健康接口正常，队列为 0。远端新视频模板检查通过。主站生产构建及生产 HTML 测试通过；首次测试漏传 `/zhijing` 基路径而返回 404，带上实际基路径重跑通过。代码检查排除生成的 `render/` 备份目录，避免将旧版压缩产物当作源码检查。
