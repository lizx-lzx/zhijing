# 问卷开头选择减法 · 2026-09-10

用户指出第 4 题的大块“平均数”示意画面与选择任务无关，增加理解和滚动负担。

本轮只改讲解入口题：移除五块预览及“呈现示意”，改为单列紧凑选项；题目改成“讲新东西时，你更喜欢怎样开头？”。保留故事、结论、整体脉络、问题、按内容决定五个既有值，默认选择、切换、返回和保存逻辑不变。不增加图片、解释小字、图标或依赖。

样式限于这一题，保留选中勾选、键盘焦点、现有短入场和减少动态支持。其他题、历史案例和生成媒体不在本轮范围。

验证：34 项单元/HTTP/组件渲染检查、类型、规范、生产构建与 1 项生产 HTML 检查通过。回归检查涵盖五个值保留、该题不再生成预览、不再携带数学示例说明。未执行浏览器视觉或点击验收，不以代码检查代替用户审美验收。

发布沿用自有服务器前端独立切换，保留上一版；不重启 API、不修改用户数据、密钥或其他服务。
