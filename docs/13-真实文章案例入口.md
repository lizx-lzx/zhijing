# 真实文章案例入口 · 2026-09-10

用户确认以此前提供并已制作的《窗口期可能只剩五年》作为学习案例，不使用通用平均数示意。

- 第 3 题的四种形式改用紧凑选择项，移除数学图表、假波形及“呈现示意”。选择下方只有一张小型真实作品入口，不自动打开或播放。
- 点击入口，在问卷内弹窗打开同一篇文章的已发布作品，可预览视频、图文、音频和互动全景图。模式参数复用案例已支持的 `video / reading / audio / overview`，不是新制作的媒体或个性化生成结果。
- 预览形式使用独立状态，不会改写问卷选项或已保存的 Skill。关闭会卸载 iframe，停止其中媒体；复用已有弹窗的 Escape、焦点恢复和滚动锁定。保留新窗口打开入口。
- 输入文章页的“平均数”试作入口也替换为这份公开成品预览，避免为看一个案例触发模型生成。任意文章正常生成流程与旧 API 兼容逻辑不改动。
- 首页原本就指向这篇文章，保持不变；第 4 题仍为简洁文字选项。历史数学案例文件不删除。

本次采用网页设计 Skill 的局部减法原则，不新增图片、依赖、供应商或托管。沿用用户指定的自有服务器，不使用 Sites 托管。作品本体、作者归属及推演提示不修改。

验证：34 项单元/HTTP/组件渲染检查、类型、规范、生产构建与 1 项生产 HTML 检查通过。新增检查覆盖四种模式映射、默认不挂载媒体、单一选中态、真实案例路径，以及问卷/输入页无数学预览。未进行浏览器视觉或点击验收，不据此声称已经审美验收。

前端独立发布，上一版本保留；不修改 API 服务、用户数据和私有配置。
