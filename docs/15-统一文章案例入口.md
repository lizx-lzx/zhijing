# 统一文章案例入口

用户要求「接着上次继续」也使用《窗口期可能只剩五年》的宣纸风格视频，并撤掉平均数、读书量旧案例；不添加其他案例和样式。

- 工作台仅保留一个文章案例入口，移至「接着上次继续」，固定先打开视频，复用上次发布的 MP4、封面与弹窗。
- 工作台、学习库的列表和计数在分页前排除旧系统样例：来源模式为 `sample` 且来源标题严格等于「平均数为什么不一定代表大多数人？」。不按用户作品标题模糊匹配；同名的用户自带文章仍保留。
- 旧样例生成入口返回 410，删除前端已无按钮使用的旧生成分支。内部测试仍可通过普通内容导入使用隔离固定材料，不暴露为产品案例。
- 旧数据库记录、笔记和媒体不删除，旧作品链接仍受所有权校验；其他私人作品的入口与续学功能保持不变。
- 不改变问卷偏好、不自动生成新内容、不调用模型或配音，不修改上次视频和主题。

验证：主站 36 项测试、类型检查、规范检查、生产构建和生产 HTML 测试通过；新增测试覆盖旧样例隐藏、同标题用户文章保留、列表计数及分页、历史记录可恢复与跨用户隔离，以及工作台只出现一个文章视频案例入口。未执行浏览器交互验收。

已发布到自有服务器 `20260910-article-only`，保留上一版 `20260910-rice-paper`。两个服务 active、API 健康、队列为 0；10 个新版公网 JS/CSS 的哈希与本地构建一致，原宣纸视频 Range 返回 206。只读计数确认存在 11 份符合旧内置样例条件的历史作品，现由列表过滤规则隐藏，记录和媒体未删除。
