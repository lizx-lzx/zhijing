# 界面减法

2026-09-08：按用户确认的范围，精简整个主平台的冗余文字与重复展示。保留图片、动效、完整内容和全部功能，不修改学习作品、个人规则或后端生成流程。

## 取舍

- 首页：删掉两层装饰口号、插画下重复的形式标签、三步流程小字和案例卡的重复形式列表。保留明确用途、主入口、题数与时间、可修改提示；回访时仍说明旧作品保留、学法保存后更新。
- 问卷：8 题、3 组、选项值、多选、前后导航均不变。开头示例只展示一次，重复的文字版本供读屏使用；形式示意去掉重复底注。保留目标与互动选项的具体例子、虚构标记、多选和自愿参与提示。
- 学法：三项偏好概览保留，重复的示意卡删除，摘要和完整可编辑规则放入展开区。保存、重新做问卷、下载与版本预览保留，仍需明确保存才生效。
- 输入：侧边配方卡和临时调整合并成文章输入框内的一个展开区，沿用已有的 `profileForLesson` 计算本次有效偏好；选中的附加形式仍可见。删除冗长的全形式枚举，保留全形式开关及提交前的授权、AI 处理告知。
- 学习：七个并列按钮改为有文字标签的原生形式选择器。仍使用原有 `switchMedium`，续看优先、默认主形式、播放器暂停和进度保存逻辑不变；旧作品仍可切换其支持的五种形式。
- 学习库与笔记：删去重复保存说明，保留搜索的无障碍标签、空状态提示与真实保存状态。

没有删除课程正文、知识结论、来源依据、事实边界、错误信息、账号恢复警示或私人学习数据。暂停动效与系统减少动态设置保留。顺带移除本次废弃模块的样式，没有新增依赖。

## 验证范围

组件渲染测试覆盖精简首页、折叠设置、完整规则、只读展示不改写学法、问卷关键提示、七种形式和旧作品。已有浏览器验收脚本同步了新的选择器，但本轮未运行浏览器交互或截图验收，不将组件测试当作实际观感验证。

本地验收：33 项单元 / HTTP / 组件检查及 1 项生产 HTML 检查通过；TypeScript、ESLint、生产构建通过。未重新调用模型或生成媒体。

已发布应用提交 `4eab136`，目录 `/home/ubuntu/apps/zhijing/releases/20260908-less-copy`。`2026-09-08T17:05:18.812Z` 主页面及 8 份 JS/CSS 资源返回正常，公网字节与本地已验证构建逐文件一致。两个旧案例继续返回 200。主样式 SHA-256 为 `091deb861373886d534506c6d9f76bc52105a2f34f5a15fd6d9162cc04f0f038`。

发布前后 API 进程均为 `3064380`，健康版本仍为 `2.0.0`，模型与配音配置正常。后端目录逐文件比较未变化，只重启网页服务；持久数据未迁移、未删除，上一版目录与静态资源保留。

入口：[从头体验精简版](https://app.chainvalley.top/zhijing/?start=welcome&ui=4eab136)。这是重新进入欢迎页，不是清除账号或学习记录。
