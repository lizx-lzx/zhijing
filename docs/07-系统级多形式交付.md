# 知径系统级多形式交付

用户在 2026-09-08 明确要求补齐知径整个系统，不是继续增加单篇 Demo 的控件。本轮完成态：正式问卷与个人 Skill → 用户提供真实文章 → 来源核对后的完整学习内容 → 图文、逐章图解、全文关系、独立听读、讲解视频、可选互动与复习 → 保存、续看、补充形式、下载。

## 执行边界

- 正式站点 `/zhijing/` 是交付入口；历史 Demo 保留，不再作为通用能力的完成证据。
- 多形式共用一份来源与知识结构，但音频用不依赖画面的独立讲稿，不把视频音轨冒充音频适配。
- 结构分析、完整编排与核对、互动编排及核对各有单独职责；只修改应用内部提示词，不装备或修改用户全局 Skill。
- 旧作品不丢失，完成的新作品可补充音视频；失败保留成功内容，只重试缺失部分。
- 学习位置、笔记与可选练习保存在私有服务端；不根据行为暗中改写个人 Skill。
- 4 核 8G 服务器继续串行制作，沿用现有模型、配音和权限；不新增收费供应商、公开原文或变更其他服务。
- “学习效果更好”、无限并发、商业收费、实名账号与异地备份不是本轮可宣称完成的事项。

## 验证

兼容旧数据、来源约束、完整性、权限隔离、状态保存与恢复、补充生成及局部失败重试；真实模型处理用户给出的文章及另一篇不同主题文章。模型内容差异不当作有效性实验证据。发布使用新版本目录、数据库一致性快照、旧版本回退，保留历史静态案例及私有媒体。

2026-09-08 已发布到自有服务器的正式入口。隔离真实生成、版本切换与公网资源验证均已完成；发布证据见本文末尾。

## 实现与验收中解决的问题

- 正式生成返回 `schemaVersion: 2`，所有新作品使用同一个主站学习页，历史 Demo 保留为独立历史作品，不再混用为通用系统的验证。
- 新增章节结论、成立条件、复习卡、独立听读讲稿；完整核心概念覆盖检查。长文可编排至 12 章，而非为固定 10 章上限遗漏内容。
- 全文分组必须完整覆盖每一章。可选章节连线若编号、来源或类型无法确认，排除该连线并展示系统说明，不编造连线、不丢失章节，也不让一个非必要候选联系阻断整份作品。
- 真实长文测试发现模型会抄错引句或猜错片段编号。改为程序生成逐字原文片段目录，模型只选片段编号，程序核对并直接提取原文；无效编号仍不能交付。结构错误提供具体缺失项，最多自动修正一次，不用假结果兜底。
- 所有模型调用带当前日期，并明确区分原文事实陈述、作者推演、平台教学虚构。人工抽查发现过“未核验被误写成未发生”的问题，因此加强边界核对；这些工程检查不等于全面事实认证。
- 多媒体分两条制作：`audio.m4a` 是视频讲解音轨，`listen.m4a` 是独立听读。已完成文件保留，未完成文件使用临时名称，验证成功再交付。
- 私有 `lesson_state` 表保存章节、两种播放时间、卡片与推演选择、笔记；参数裁剪与账户权限检查。原作品、个人 Skill、学习行为分别存储，不暗中改写长期学法。
- 学习库增加标题搜索和 24 条分页，旧作品仍可读，重新生成做独立副本；补充音视频不重做正文，重复请求已完成媒体不重复扣生成次数。
- 离线 HTML、Markdown 和 JSON 提供学习内容、原文依据与笔记；媒体和逐章 SVG 独立下载。
- 浏览器点击和截图测试脚本已同步新入口，但本轮没有执行浏览器交互验收；本轮使用组件服务端渲染、真实 HTTP、数据库重启、文件校验和媒体验证。

## 2026-09-08 发布证据

- 正式入口：<https://app.chainvalley.top/zhijing/>，不是 `/demo/`。应用提交 `778be80`，验收修正 `3b07b9d`、`daaf5f8`。当前版本目录 `/home/ubuntu/apps/zhijing/releases/20260908-system-v2`，前端与 API 常驻服务均为 active，公网 API 版本 `2.0.0`。
- 最终运行 `NEXT_PUBLIC_BASE_PATH=/zhijing npm test`：29 项单元 / HTTP / 组件渲染检查及 1 项生产页面检查全部通过，TypeScript、ESLint、生产构建通过。
- 三组真实模型验收通过。用户长文的分析入口与故事入口各生成 12 章，首章分别为“核心结论：窗口期可能只剩五年”和“2026 年春节的 AI 风暴”；不同主题的全貌入口生成 9 章。每份作品完整分组覆盖全部章节，并包含来源、术语和可选推演。
- 长文视频 167.68 秒，独立听读 292.64 秒，声音文件与逐章字幕均存在、可私有范围读取。不同入口的工程差异已验证，不把它解释为学习效果优劣的实验。
- 对已完成的 9 章作品补充 249.92 秒独立音频后，正文哈希与个人笔记不变；重复补充已完成形式不重新生成。学习状态、导出、跨账号隔离与恢复码流程通过。脱敏报告保存在本地 `test-results/system-acceptance.json`。
- 曾在长文音频制作中重启隔离 API。重启后原有内容、来源结构与已验证声音文件哈希不变，未完成部分继续完成；`scripts/restart-proof.mjs verify` 返回 `RESTART_VERIFIED`。
- 发布前数据库一致性快照位于私有目录 `/home/ubuntu/zhijing-db-before-system-v2.z8WOfM/zhijing.sqlite`。逐条只读对比确认此前 11 个访客、5 份个人学法、7 个学法版本、4 份来源与 6 份作品全部保留且内容未变；迁移至版本 2，SQLite `quick_check=ok`。旧发布目录保留，可回退服务但不以旧快照覆盖新数据。
- 公网 HTML 引用的 8 份 JS / CSS 均返回 200，逐文件 SHA-256 与本地生产构建相同；历史两个 Demo 仍可访问。旧知乎案例视频 SHA-256 仍为 `d86ae71205eb7a8bb512ca84a99233c6ea5840e2235438150c5731a807b6e47a`。
- 公网真实全套生成于 `2026-09-08T16:03:30.860Z` 通过：作品 `65bffa5508dd82bd53994a8ceef575a8`，视频 55.52 秒、独立听读 141.76 秒。真实个人 Skill → 正文编排 → 视频 / 听读 / 图文 / 网页讲解 → 笔记保存 → 私有范围读取 → 导出 → 恢复码恢复均通过，跨访客访问返回 404，跨站写入被拒绝。脱敏记录为本地 `test-results/public-smoke.json`；该作品属于私有验收访客，不注入真实用户的学习库。
- 隔离验收 API 与网页服务已停止，保留私有测试数据和证据；主站两个服务继续运行，未修改其他项目、Nginx 或供应商配置。

## 仍然有效的边界

工程验收不等于全面事实认证、教学有效性试验或真实浏览器全交互验收。早期长文验收作品中出现过把“未外部核验”写成“未发生”的措辞；后续系统提示和核对规则已经修正，原验收作品作为历史证据保留，没有悄悄改写成新的通过结果。新生成也仍须保留作者判断、来源事实陈述与教学虚构之间的边界。

本轮完成的是小规模可用的主站学习闭环。真人账号找回、异地备份、付费运营、大规模并发以及“更符合偏好是否真的提升学习效果”的用户实验，仍不在已完成范围。
